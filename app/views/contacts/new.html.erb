<p>contacts</p>

<%= form_for :contact, url: contacts_path do |f| %>
	<p> 
	<%= f.label :Enter_email %><br/>
	<%= f.text_field :email %>
	</p>

	<p> 
	<%= f.label :Entrer_message %><br/>
	<%= f.text_area :message %>
	</p>
	
	<p>
	<%= f.submit %>
	</p>
<% end %>